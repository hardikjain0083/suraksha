# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://<user>:<pass>@cluster0.xxxxx.mongodb.net/suraksha_maps?retryWrites=true&w=majority&appName=Cluster0

# JWT Authentication
JWT_SECRET=your-jwt-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Security
BACKEND_CORS_ORIGINS=["https://your-frontend.vercel.app","http://localhost:5173"]

# Application Settings
ENVIRONMENT=development
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Set DEMO_MODE=true only for hackathon demo with judges
# Leave empty or false for production behavior
DEMO_MODE=false
