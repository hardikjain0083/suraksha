# Evidence mock files

Place sample evidence files here (PDF scans, JSON configs, screenshots) for department upload demos.

Run `python backend/generate_mock_pdfs.py` to generate sample PDFs into this folder if needed.
